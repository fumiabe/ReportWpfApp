<UserControl x:Class="Common.ReportItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Common"
             xmlns:prop="clr-namespace:Common.Properties"
             x:Name="parent"
             mc:Ignorable="d" d:DesignWidth="200" d:DesignHeight="50">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Common;component/Style/ReportItem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid DataContext="{Binding ElementName=parent}">
        <StackPanel>
        <Border>
            <Label Content="{Binding Path=LabelContent}"/>
        </Border>
            <TextBox Text="{Binding Path=TextContent}" Width="{Binding Path=TextBoxWidth}"/>
        </StackPanel>

    </Grid>
</UserControl>
